type User {
  id: ID! @id
  name: String!
}

type Image {
  id: ID! @id
  name: String!
  path: String!
  file: Json!
}

type Video {
  id: ID! @id
  title: String!
  path: String!
  file: Json!
}

type Worship {
  id: ID! @id
  title: String!
  content: String!
  file: Json!
  createAt: String!
}

type Life {
  id: ID! @id
  title: String!
  content: String!
  file: Json!
  createAt: String!
}

type Story {
  id: ID! @id
  title: String!
  content: String!
  file: Json!
  createAt: String!
}